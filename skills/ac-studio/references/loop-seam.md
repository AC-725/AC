# Loop seam without authoring the open twice

Render the whole composition as a pure function of an explicit `t`, then render it twice: once
at `t=T`, once at `t=0` with opacity ramping over the last 0.3s. The sign-off dissolves into
frame one, and there is only one copy of the opening choreography to maintain.
