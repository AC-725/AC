# Brand — the single source of truth

Last updated: 2026-07-26

This file ends arguments. If a skill states a brand fact that contradicts this page, the skill is
wrong — fix the skill. Never restate a value from here in a skill; point at this file instead.

## Identity

- **Brand:** AC — *AI in Plain English*
- **Person:** Austin, goes by AC
- **Promise:** making AI make sense for normal businesses. One real AI trend a day, plain English,
  zero jargon.
- **Positioning:** the antidote to AI hype. Every piece of content builds one reputation —
  *"this person makes AI make sense."*
- **Feeling:** premium, calm, confident. Black canvas, one gold idea, elegant serif headline.
  "Quiet luxury," not "hustle bro."

## Handle — one value, no alternates

**Instagram: `@itsac.ai`**

This is the handle every Instagram surface optimizes for: profile, bio, on-frame watermark, every
CTA, every caption sign-off, and all growth and SEO work.

Do not list backup or alternate handles anywhere. A primary-plus-backups list is exactly what caused
the earlier split — `@ac.wins` was recorded as primary in `ac-instagram` while `ac-reel-creator`
defaulted to `@itsac.ai`, and reels shipped under both. One value, one place, no options.

`@ac.wins` is **retired.** It should not appear in any skill, template, caption, or cover. Treat any
new occurrence as a bug.

## Website

**`itsac.ai`** — moving to match the handle. Austin confirmed the move on 2026-07-26; `ac-wins.com`
is the old home base.

Two caveats, both live:

- **Confirm the exact spelling.** This was recorded as `itsac.ai` to match `@itsac.ai`. If the real
  domain differs (a hyphen, a different TLD, a subdomain), correct it here first — everything else
  points at this file.
- **Don't switch the live bio link until the site actually resolves.** "Moving" is a transition, and
  a bio pointing at a domain that doesn't load costs more traffic than an old domain that does.
  Check it loads, then switch. A link hub (Linktree / Stan) is a fine interim target.

Note this is distinct from the **AC Wins** client-services business (the `ac-wins-*` skills). Those
never referenced this domain and are unaffected.

## Palette — gold is the only accent

| Hex | Name | Where |
|-----|------|-------|
| `#0A0A0A` | Base Black | background of every frame |
| `#16130E` | Warm Charcoal | gradient depth |
| `#E7C765` | Signal Gold | primary accent & monogram |
| `#C9A23F` | Deep Gold | gradient mid-tone |
| `#F4DF95` | Light Gold | gradient highlight / sheen |
| `#F4F0E7` | Warm White | main headline text |
| `#B9B1A1` | Muted Stone | body / supporting text |

Gold gradient: `linear-gradient(118deg,#F4DF95,#E7C765 46%,#C9A23F)`.

## Type — two fonts, never more

- **Lora** — headlines and big numbers. High-contrast serif; this is the "luxury" in black and gold.
- **Poppins** — kickers, labels, body. Weights 400–700 for both.

## Voice

- Direct and plain-spoken. Short sentences. If a 12-year-old couldn't follow it, rewrite it.
- Contrarian but constructive. Challenge the obvious take, then give a better one. Never doom.
- Anchored to one real, current number. The number earns attention; plain English keeps it.
- Encouraging, not preachy. Anti-hype, pro-action.

## Non-negotiables

- One gold accent per scene. Gold means "look here."
- No em-dashes on screen, ever. Period, comma, or line break.
- No buzzwords, no "this changes everything," no fear-mongering.
- ~30 words max per frame; keep the black breathing room.
- No other colors, stock photos, or clip-art. Name a company with a clean gold wordmark, never a
  multicolor logo.
- `@itsac.ai` on every frame. Never two handles on one asset.
