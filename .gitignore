# Firecrawl CLI scratch output (scrape/crawl results)
.firecrawl/

# Never commit credentials. EXA_API_KEY / FIRECRAWL_API_KEY belong in the
# environment's variable settings, not in the repo.
.env
.env.*
!.env.example
ac-studio.skill

# Generated site assets are committed deliberately — see kiradesign/ART-DIRECTION.md
!kiradesign/assets/
kiradesign/node_modules/
kiradesign/deck/preview/
kiradesign/deck/pdf-preview/
kiradesign/dist/
kiradesign/shots/
